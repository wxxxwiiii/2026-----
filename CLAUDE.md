# 高校体育教师 AI 教学智能体

面向高校体育教师的**单机网页端** AI 教学辅助平台（无学生端），覆盖课前备课、课中教学、课后分析。

## 当前阶段
- 阶段 4 智能备课台：第 1 步（首页 5 卡片 + AI 快捷对话抽屉 + 多服务商 AI 客户端）已完成。
- 备课台后续：教材资源库 → AI 备课中心 → 教案编辑版本 → 分层作业 + 成果管理。
- 阶段 3 体质健康分析 仍待开发（用户先安排了备课台）。

## 工作说明（每次开始工作前先读）
1. **增量推进**：只做当前阶段规划的事，不做超出范围的大改。
2. **每阶段验收**：完成一个阶段后停下，向用户汇报「做了什么、怎么验证、下一步」，等用户确认后再继续。
3. **每日开发日志**：每次工作结束，更新 `开发日志/YYYY-MM-DD.md`（已完成 / 待办），并同步 `开发日志/TODO.md` 滚动待办。
4. **文档先行**：需求、技术、设计有变化时，先更新 `docs/` 再写代码。
5. **安全推进**：每阶段保持可运行、可回退；数据库在 `data/app.db`，备份 = 直接拷贝 `data/` 目录。

## 标准文件路径索引
| 内容 | 路径 |
|---|---|
| 需求文档 | `docs/01-需求文档.md` |
| 技术方案 | `docs/02-技术方案.md` |
| 设计规范 | `docs/03-设计规范.md` |
| 数据库设计 | `docs/04-数据库设计.md` |
| 开发计划与执行步骤 | `docs/05-开发计划与执行步骤.md` |
| 运行与验证指南 | `docs/06-运行与验证指南.md` |
| DeepSeek 密钥申请指南 | `docs/07-DeepSeek密钥申请指南.md` |
| 每日开发日志 | `开发日志/`（按日期） |
| 滚动待办 | `开发日志/TODO.md` |

## 关键约定
- 主色调：**米色为主 + 淡蓝点缀**，界面简洁直观。
- 后端：Python + Flask；数据库：SQLite（SQLAlchemy）；前端：HTML + CSS + 轻量 JS（Flask 模板）+ ECharts。
- AI：DeepSeek（文字 `deepseek-chat`，视觉 `deepseek-v4-flash-vision-exp`）。
- 运行：Windows 上双击 `start.bat` 启动，单机版，数据存本机 `data/`。
- 代码注释与文档一律使用中文。
